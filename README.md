# updatable_webring_template
MVP for a 'webring' - structured set links to other (scientific) data - which can be updated (new content added + site rebuilt) via some external source (in this case, google sheets).
